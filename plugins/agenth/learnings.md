# Technical Learnings

Solutions discovered during task execution. Auto-populated by Claude when problems are solved.

---

<!--
FORMAT:
## [DATE] Short problem description
**Problem**: What went wrong / the error encountered
**Solution**: The fix that worked
**Trigger**: When this typically happens (optional)
**Tags**: #relevant #tags

EXAMPLE:
## [2025-11-22] npm phantom dependency issue
**Problem**: `npm install` silently keeps old cached versions despite package.json changes
**Solution**: `npm cache clean --force && rm -rf node_modules && npm install`
**Trigger**: After major version bumps, before debugging weird import errors
**Tags**: #npm #dependencies #cache
-->
