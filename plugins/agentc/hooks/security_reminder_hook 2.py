#!/usr/bin/env python3
# Auto-generated by context-lock - executes locked script
import subprocess, sys, os
script_dir = os.path.dirname(os.path.abspath(__file__))
locked_file = os.path.join(script_dir, 'security_reminder_hook.py.locked')
result = subprocess.run(
    ['context-lock', 'exec', locked_file, '--key', '/Users/db/.config/context-lock/keys/agentc', '--'] + sys.argv[1:],
    capture_output=False
)
sys.exit(result.returncode)
